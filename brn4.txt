change networkRequest
send client requests (make enum)
send server response (make enum)
create map file
load map file
send map file over network
simple map editor
cmd loadMap
cmd editMap
cmd saveMap

move curser
place tile
place item
place entity

calculate light
define roof
define areas

/setWindow
/musicVolume
/CmdParser
/SetWindow




NPC:

OnInteract (Player p) {
SayTo(p, "Welcome traveler!");
GiveTo(p, GameLoader.Instance.GetItem("rusty_sword"));
}