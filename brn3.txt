every entity gets a tick as well

class Mob : Entity
AiState aiState;

void Tick () {
if( CheckRange(4,4) )
aiState = agressive;
//markChanged(this);

if( aiState == idle)
if(Rnd((float f) => f>.5f))
Move(RndDirection() );
NetworkHandler.Instance.MarkChanged( this);
}

GlobalSettings:

worldTime
season
year
